// Package {{.Package}} — Atom (L1)
// 纯函数：输入 {{.Input}} → 输出 {{.Output}}
package {{.Package}}

// {{.Name}}Atom 执行纯计算，无副作用。
// input: {{.Input}} → output: {{.Output}}, error
func {{.Name}}(input {{.Input}}) ({{.Output}}, error) {
    // TODO: 实现业务逻辑
    var zero {{.Output}}
    return zero, nil
}
